# Set React dev server port to 3000 (backend is running on 8080)
PORT=3000

# Set browser to open on start. On Windows use the full path to chrome.exe.
# Update the path below if your Chrome is installed elsewhere.
BROWSER="C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe"
